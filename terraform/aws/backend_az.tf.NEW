terraform {
  backend "azurerm" {
    resource_group_name   = "tstate"
    storage_account_name  = "tstatechrisley"
    container_name        = "tstate"
    key                   = "terraform.tfstate"
  }
}

